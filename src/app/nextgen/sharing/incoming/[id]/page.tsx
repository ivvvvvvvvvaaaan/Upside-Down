import { PlaceholderView } from '@/components/layouts/placeholder-view'

export default function IncomingSharePage({ params }: { params: { id: string } }) {
  return <PlaceholderView title="Incoming Share" section="Sharing" />
}
